package com.revature.proj1.datalayer;

import java.util.HashMap;
import java.util.Map;

import com.revature.proj1.beans.Employee;

//The implementing interface will perform operations on Employee table ONLY
//this will call a separate class, ReimbursementDAO, to create and store
//the corresponding reimbursement request
//A manager DAO implementation will extend this implementation, giving a manager
//all the same methods herein, as well as methods to approve a reimbursement

/*
public static ConnFactory cF = ConnFactory.getInstance();
static Logger logger = (Logger) LogManager.getLogger(); */
public interface EmployeeDAOInterface {
	
	
}
